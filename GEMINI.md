# Project Guidelines & Rules

## Git Commit and Push Restrictions
- **NEVER** automatically execute `git commit` or `git push` commands unless the user has specifically and explicitly instructed you to do so in their prompt.
- Always keep changes local and wait for explicit confirmation before committing or pushing to any remote repository.
